{
  "project": "Result-finder",
  "branchName": "v2",
  "description": "GPA Projection simulator enhancements including fail-aware grade inputs, checkbox elective selection with hard caps, and accurate GP=0.00 calculations.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Fail-Aware Grade Inputs",
      "description": "As a student using the GPA Projection simulator in detailed mode, I want to select future semester course grades using a slider that only allows 0.00 (Fail) and grades >= 2.00 to 4.00, so that my projections reflect valid academic grades.",
      "acceptanceCriteria": [
        "Replacing the detailed mode number_input with a select_slider in analytics.py.",
        "Slider options: [0.00, 2.00, 2.25, 2.50, 2.75, 3.00, 3.25, 3.50, 3.75, 4.00].",
        "Display 0.00 formatted as 'F' and other values as floating points with 2 decimal places.",
        "Summary mode remains untouched."
      ],
      "priority": 1,
      "passes": true,
      "notes": "Replaced number_input with select_slider. Standardized valid GPA points to [0.00, 2.00, 2.25, ..., 4.00]. Formatted 0.00 as 'F'."
    },
    {
      "id": "US-002",
      "title": "Checkbox Course Selection & Hard Cap",
      "description": "As a student in final semesters (CSE 7th & 8th, Civil 8th) where there are too many electives, I want to select which electives I am actually taking via checkboxes and have a hard cap limit that prevents selecting more credits than the semester limit, so that my projection is accurate.",
      "acceptanceCriteria": [
        "database.py get_semester_courses() has an include_all_electives flag returning all actual electives.",
        "Identify CSE 7/8 and Civil 8 semesters as having checkbox-gated electives.",
        "Retrieve the semester's credit cap (e.g. 17.5 for CSE 8th, 17.5 for Civil 8th).",
        "Render electives with checkboxes (unchecked by default). Core courses are automatically checked/included.",
        "Disabling unchecked checkboxes when total selected credits equal the semester credit limit (hard cap).",
        "Completely exclude unchecked electives from the GPA/credits computation."
      ],
      "priority": 2,
      "passes": true,
      "notes": "Added include_all_electives parameter to get_semester_courses(). Implemented checkboxes in analytics.py with pre-pass credit sum to disable checkboxes once cap is hit."
    },
    {
      "id": "US-003",
      "title": "Correct GPA Calculation for Fails",
      "description": "As a student, I want failed courses (GP=0.00) that are selected in my projection to still count their credits toward the semester total, so that they correctly drag down my CGPA.",
      "acceptanceCriteria": [
        "In database.py compute_graduation_cgpa_from_inputs, include courses if credit > 0, even if gp == 0.",
        "Ensure live GPA calculations in analytics.py also count credits for selected courses with GP = 0.00."
      ],
      "priority": 3,
      "passes": true,
      "notes": "Modified compute_graduation_cgpa_from_inputs and analytics.py to always include credits of selected courses regardless of grade."
    },
    {
      "id": "US-004",
      "title": "N+1 Database Query Bottleneck Optimization",
      "description": "As a system administrator, I want the get_profiles() function to fetch student lists in a single batched query instead of iterating database requests, so that dashboard loading performance is optimized.",
      "acceptanceCriteria": [
        "Refactor database.py get_profiles() to retrieve all profiles in one query, then retrieve all student rosters in a second single query, and assemble the profiles dictionary in memory.",
        "Eliminate the N+1 database queries sequential loop."
      ],
      "priority": 4,
      "passes": true,
      "notes": "Refactored get_profiles to load all profile students in a single batched query instead of executing a sequential N+1 query loop."
    },
    {
      "id": "US-005",
      "title": "Hosted Streamlit Cloud Performance & Caching Optimization",
      "description": "As a student using the dashboard online, I want UI interactions to be instant without waiting for network or database reads, so that the hosted experience matches local performance.",
      "acceptanceCriteria": [
        "Wrap fetch_exams() inside cli_scraper.py with an hourly SQLite-backed metadata cache.",
        "Implement Streamlit memory cache decorators (@st.cache_data) for fetch_exams() and get_profiles() in app.py, pages/analytics.py, and pages/transcript.py.",
        "Eliminate direct network-blocking requests on selectbox changes and UI interactions.",
        "Maintain complete data integrity and automatic cache clearing on database writes."
      ],
      "priority": 5,
      "passes": false,
      "notes": "Declined caching because Streamlit's @st.cache_data Pickle serialization of large nested dictionaries degraded hosted CPU performance."
    },
    {
      "id": "US-006",
      "title": "Batch Student Data Queries Optimization",
      "description": "As a student using the analytics dashboard, I want the statistics and student cards to load instantly without sequential SQL loop delays, so that my usage is seamless.",
      "acceptanceCriteria": [
        "Refactor database.py get_student_data_for_exam() to fetch all student grades for this exam in a single bulk query and map them in-memory, completely eliminating the sequential database query loop.",
        "Ensure all GPA and CGPA calculations, fallback rules, and status mapping continue to function perfectly with 100% test coverage."
      ],
      "priority": 6,
      "passes": true,
      "notes": "Refactored get_student_data_for_exam to load student grades and historical best grades in single bulk queries, completely eliminating N+1 sequential loops in the student loop."
    },
    {
      "id": "US-007",
      "title": "Database connection pooling and data loading optimizations",
      "description": "As a developer, I want to optimize database connection efficiency, index seeks, and data parsing, so that the page load time is reduced by 30-50% on Streamlit Cloud.",
      "acceptanceCriteria": [
        "Implement a thread-local SQLite connection pool in database.py returning reusable connections mapped by DB_PATH.",
        "Ensure context manager ClosedOnExitConnection commits but does not close pooled connections.",
        "Add compound indices on (profile_name, exam_id) for subject_grades and exam_results tables.",
        "Eliminate WAL checking from get_connection() and move it to init_db().",
        "Eliminate the redundant get_profiles() call in pages/analytics.py.",
        "Pre-filter retakes and improvements in SQL inside get_longitudinal_data().",
        "Move defaultdict imports to top-level in database.py."
      ],
      "priority": 7,
      "passes": true,
      "notes": "Implemented thread-local SQLite connection pool in database.py returning reusable connections mapped by DB_PATH. ClosedOnExitConnection modified to commit/rollback transactions without closing pooled connections. Moved persistent WAL initialization to init_db(). Added optimized compound indices on (profile_name, exam_id) to eliminate full-table scans. Optimized longitudinal retake queries with SQL-level NOT LIKE pre-filtering. Removed redundant get_profiles() scans in analytics.py. Cleaned up global defaultdict imports."
    }
  ]
}
