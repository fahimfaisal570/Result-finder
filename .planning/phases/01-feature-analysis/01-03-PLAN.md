# Phase 01 Plan 03: Drop Unneeded Features from Production Predictor

## Goal
Simplify the ML model feature vector from 6 features to 3 features (`last_gpa`, `difficulty`, `backlog_count`) in `ml_predictor.py` and update associated tests to ensure complete correctness and compatibility.

## Proposed Changes
1. **Modify `ml_predictor.py`**:
   - Update `engineer_features` to return only 3 features: `last_gpa`, `difficulty`, `backlog_count`.
   - Update default shape from `(0, 6)` to `(0, 3)` in `build_training_data`.
2. **Modify `tests/test_ml_predictor.py`**:
   - Update expected feature dimensions and values in `test_engineer_features_clipping`, `test_build_training_data_and_ensemble`, and `test_forecast_to_graduation`.

## Verification Plan
Run pytest on `tests/test_ml_predictor.py` to verify all tests pass.
