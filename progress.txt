# Ralph Progress Log
Started: Wed May 27 16:05:00 2026
---
## Codebase Patterns
- Streamlit application using a multi-page setup under `pages/`.
- UI modules and helpers defined in `ui_components.py`, analytics in `pages/analytics.py`, and DB operations in `database.py`.
- Course data is fetched from database or loaded dynamically.
- Graduation CGPA calculations are done in `database.py` (`compute_graduation_cgpa_from_inputs`) and previewed live in `pages/analytics.py`.

## 2026-05-27 16:05 - Initialization
- Initialized `prd.json` and `progress.txt` for tracking.
- Set up tasks for US-001, US-002, and US-003.

## 2026-05-27 16:15 - Completion of US-001, US-002, and US-003
- Implemented `select_slider` for Detailed mode course grade inputs, restricting valid grades to `[0.00, 2.00, 2.25, ..., 4.00]` with `0.00` formatted as "F".
- Modified `database.py`'s `get_semester_courses()` to support returning all actual electives with `include_all_electives=True`.
- Developed checkbox-gated elective selection in `pages/analytics.py` for CSE 7/8 and Civil 8.
- Added a pre-pass in Streamlit to dynamically sum selected credits and enforce a hard credit cap by disabling unchecked checkboxes once the cap is reached.
- Excluded unchecked electives from the CGPA/credits calculations.
- Corrected CGPA calculation logic in `database.py` and `pages/analytics.py` to ensure failed courses (GP=0.00) properly add their credits to the denominator.
- Wrote extensive new unit tests in `tests/test_database.py` to cover both `include_all_electives` and accurate GP=0.00 calculations.
- All 15 unit tests passed successfully.
- Modified files: `database.py`, `pages/analytics.py`, `tests/test_database.py`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Using Streamlit session state and rendering a pre-pass loop allows flawless computation of dynamic attributes (like disable states based on accumulated checkboxes) before any widgets are rendered, eliminating widget initialization lag or logic ordering bugs.
  - Ensuring the calculator evaluates selection status by `credit > 0` instead of grade values guarantees failures are properly included in the total credit calculations.
---
## 2026-05-29 09:59 - Completion of US-004
- Optimized `database.py`'s `get_profiles()` by eliminating the sequential N+1 query loop.
- Instead of querying the `students` table once for each profile, we now fetch all profiles in one query, then fetch all student records in a single bulk query, mapping them in-memory.
- Verified that all functionality remains perfectly intact by running the complete test suite.
- All 15 unit and system tests passed successfully.
- Modified files: `database.py`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Standardizing sequential query patterns to bulk loads significantly reduces round-trip database latency, especially when dealing with nested structures (like a list of profiles containing lists of students).
  - Leveraging in-memory dictionary routing (`profile_name in profiles`) allows O(1) matching after doing bulk fetching.
---
## 2026-05-29 10:20 - Completion of US-006
- Optimized `database.py`'s `get_student_data_for_exam()` by replacing sequential N+1 query loops with bulk queries.
- Created `grades_by_student` bulk query to load all subject grades for a specific profile and exam at once.
- Created `historical_grades_by_student` bulk query to load the historical maximum grade points up to a specific exam ID for all student records in the profile at once, preventing sequential calculations.
- Developed a comprehensive unit test `test_get_student_data_for_exam_optimized` in `tests/test_database.py` that validates fallback behaviors, failing grade detections, and GPA audit drift calculations.
- All 16 unit and full-system tests passed successfully.
- Modified files: `database.py`, `tests/test_database.py`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Grouping SQLite operations by `reg_no, subject_code` in a single query with aggregate functions (`MAX(grade_point)`) perfectly recreates the dynamic student fallback history in a single trip.
  - SQLite's `INSERT OR REPLACE` deletes the conflicting row, which cascaded via foreign keys and wiped student records. Seeding testing student datasets in a single batch prevents unexpected cascade deletions during unit test setup.
---
## 2026-05-29 10:39 - Completion of US-007
- Implemented a thread-local SQLite connection pool keyed by `DB_PATH` inside `database.py`.
- Refactored `ClosedOnExitConnection` so it executes `__exit__` transaction committing/rolling back but does not close pooled connections, saving over 150 ms of open/close/PRAGMA cycles per analytics page load on Streamlit Cloud.
- Shiftedpersistent `PRAGMA journal_mode = WAL` initialization to `init_db()`.
- Created compound database indices `idx_subject_grades_exam` and `idx_exam_results_exam` on `(profile_name, exam_id)` to convert heavy analytics and JOIN queries from full-table scans to instant index seeks.
- Removed redundant `db.get_profiles()` loading in Section 3.4 of `pages/analytics.py` and reused the module-level dictionary in-memory.
- Optimized longitudinal data query with SQL-level retake exclusion pattern `LIKE` filtering in `get_longitudinal_data()`.
- Moved `defaultdict` imports to top-level to eliminate repetition overhead.
- All 16 unit and full-system tests passed successfully.
- Modified files: `database.py`, `pages/analytics.py`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Indexing search attributes (`profile_name, exam_id`) used in JOINs or high-frequency filter statements eliminates costly full-table scans.
  - Custom context managers wrapping pooled connections can manage database transactions properly while preserving active connections in thread-local cache.
  - Pre-filtering entries at the database engine level (such as SQLite `LIKE` checks) reduces Python string processing overhead and drastically lowers overall response times.
---
## 2026-05-29 10:55 - Completion of US-008
- Refactored `get_cross_batch_comparison()` in `database.py` to replace the N+1 loop with two bulk database queries (matching exams using `IN`, and matching GPAs using dynamic `OR` / `IN`), reducing connection overhead and multi-cohort query time.
- Replaced the index-invalidating `CAST(exam_id AS INTEGER)` in `get_student_data_for_exam()`'s historical grades subquery by pre-fetching the list of relevant `exam_ids` from `scan_log` first in Python and using an index-friendly `exam_id IN (...)` clause, preserving compound index seeks on `idx_subject_grades_exam`.
- Optimized `get_effective_cgpa_per_student()` latest CGPA subquery to utilize a modern single-scan `ROW_NUMBER() OVER (...)` window function rather than a double-scan self-join, eliminating database self-join query loops.
- Cleaned up redundant and nested inline imports across the codebase (`statistics`, `base64`, `json`, `re`, and `numpy`), moving them to respective top-level module scopes.
- Developed a new unit test `test_get_cross_batch_comparison_bulk` inside `tests/test_database.py` to verify correctness of bulk comparison metrics.
- All 17 unit and full-system tests passed successfully.
- Modified files: `database.py`, `pages/analytics.py`, `pages/results.py`, `app.py`, `tests/test_database.py`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Standard SQL functions like `CAST` or `LOWER` in `WHERE` clauses invalidate indexing seeks, leading to full-table scans. Moving filtering logic to Python and passing precise lists via `IN` clauses preserves index efficiency.
  - Windows functions like `ROW_NUMBER() OVER` allow SQLite to partition and rank records in a single database read pass, which is significantly faster than standard nested self-joins.
  - Doing bulk checks (such as using an `IN` clause for matching sets) avoids sequential query loops, lowering response latency under high network/database latency constraints.
---
## 2026-05-29 11:36 - Completion of US-009
- Refactored `get_retake_success_stats()` in `database.py` to use a `ROW_NUMBER() OVER (PARTITION BY reg_no, subject_code ORDER BY CAST(exam_id AS INTEGER) ASC)` window function, ensuring `first_gp` always reflects the chronologically first attempt regardless of grade ordering.
- Updated `get_longitudinal_data()` semester parsing: when `sem_in_yr > 2`, the raw consecutive number is used directly as `sem_num` instead of the bi-semester formula `(yr-1)*2 + sem_in_yr`, correctly handling Civil Engineering consecutive numbering (e.g. "3rd Year 6th Semester" → sem_num=6, not 10).
- Added year title omission fallback: when no year token is present but a semester token is found, `sem_num` is set directly from the semester number (e.g. "5th Semester" → sem_num=5).
- Added `sem_num=0` filtering in `get_longitudinal_data()` sorting loop to prevent unparseable exam names (orientation, thesis, etc.) from anchoring at position-0 and corrupting `np.polyfit` slope calculations.
- Cleaned up Streamlit code in `pages/analytics.py` by moving `@st.cache_data` for `load_retake_stats` from inside the tab block to the module level.
- Refactored student trajectory metrics in `pages/analytics.py` to use `semester_num` as the actual independent variable for `np.polyfit` trajectory slope, and bounded the consistency score to `>= 0.0` to prevent invalid negative values.
- Added 3 new unit tests to `tests/test_database.py`:
  - `test_longitudinal_data_civil_consecutive_semesters`: Seeds Civil "3rd Year 6th Semester" exam and asserts `sem_num=6`, not `10`.
  - `test_retake_success_stats_chronological_first_attempt`: Seeds two subject_grades rows for same student and subject (exam_id 6001→0.0 gp, exam_id 6002→3.0 gp) and verifies `first_gp=0.0`, `best_gp=3.0`, `passed_after_retake=True`. Also verifies the reverse case (pass first, retake lower) gives `passed_after_retake=False`.
  - `test_get_longitudinal_data_parsing_and_filtering`: Seeds standard, consecutive, year-omitted, and unparseable exams, and verifies that `sem_num=0` is successfully excluded and correct sorting order is preserved.
- Full suite: **20/20 tests passed**.
- Modified files: `database.py`, `pages/analytics.py`, `tests/test_database.py`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Window functions (`ROW_NUMBER()`) are essential when "first chronologically" semantics differ from "minimum/maximum value" semantics. Using `MAX(CASE WHEN rn_asc = 1 THEN grade_point END)` cleanly extracts the first-window value in a single pass.
  - Civil Engineering uses consecutive semester numbers starting from 1 per year rather than resetting to 1. A simple threshold check (`sem_in_yr > 2`) correctly distinguishes consecutive numbering from standard bi-semester cycles.
  - Bounding computed metrics (like consistency scores) to valid ranges (`>= 0.0`) prevents downstream rendering bugs caused by negative mathematical results.
  - Filtering out unparseable semester data (`sem_num=0`) is critical when performing mathematical regression, as a data point at origin zero will severely skew slope calculations.
  - Avoid defining caching functions inside conditional rendering blocks or functions in Streamlit; module-level caching is much cleaner, reliable, and prevents warnings or repeated definitions.
---
## 2026-05-29 12:55 - Completion of US-010
- Extracted the student card loop in the GPA Projection tab (Tab 6) of `pages/analytics.py` into a standalone card helper `render_student_projection_card()` decorated with `@st.fragment` for fragment-level rerun isolation.
- Implemented roster pagination (10 students per page) in Tab 6 of `pages/analytics.py` with dynamic Previous and Next page button selectors, capping active widgets serialization and WebSocket bandwidth overhead.
- Removed redundant full-page reruns on clicking Deep Analysis or interactive sliders/checkboxes.
- All 20 database and system integration tests passed successfully.
- Modified files: `pages/analytics.py`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Rendering large loops of cards with nested containers, columns, inputs, and sliders creates 1,000+ Streamlit elements on a single page run, triggering massive WebSocket serialization lags (3+ seconds). Implementing simple 10-item pagination cuts page loads and reruns to milliseconds.
  - Wrapping dynamic cards in `@st.fragment` ensures any widget interaction (such as checkboxes, selectboxes, or button clicks) only triggers a local fragment-level rerun. The rest of the page—including invisible tabs, Altair charts, and other student cards—is completely bypassed, dropping interaction latency from 3+ seconds to <10ms.
---
## 2026-05-29 16:30 - Completion of US-011
- Optimized the GPA Projection tab (Tab 6) rendering performance to under 1 second by implementing fragment-scoped reruns.
- Replaced two `st.rerun()` calls inside `render_student_projection_card` with `st.rerun(scope="fragment")` (Retry button at line 1336 and Deep Analysis button at line 1843).
- Wrapped the pagination controls and active card rendering in a new fragment-isolated helper `render_paginated_projections` decorated with `@st.fragment`.
- Replaced two `st.rerun()` calls inside page navigation buttons with `st.rerun(scope="fragment")` (lines 1873 and 1883).
- Verified that all unit and system integration tests pass successfully (20/20 tests).
- Modified files: `pages/analytics.py`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Calling `st.rerun()` with no arguments inside a `@st.fragment` overrides the local isolation and triggers a full page rerun, re-rendering all other widgets, tables, and Altair charts, which negates the fragment-level performance benefits. Passing `scope="fragment"` confines the rerun to the innermost active fragment.
  - Nesting fragments (`render_student_projection_card` inside `render_paginated_projections`) is highly effective: changing a slider inside a card only reruns that specific card (<10ms), while navigating pages reruns the outer page container but does not affect the rest of the application.
---
## 2026-05-29 17:15 - Completion of US-012
- Implemented concurrent connection pre-warming function `warm_connection_pool(num_connections=6)` in `cli_scraper.py` using `concurrent.futures.ThreadPoolExecutor` to initiate 5 parallel HEAD requests + 1 GET request to BASE_URL to pre-establish SSL/TLS handshakes and cookies concurrently.
- Integrated `warm_connection_pool` in `run_batch_scan_engine()` as an automatic fallback if session cookies are absent, ensuring connections are pre-warmed even in CLI runs.
- Reduced the launch stagger delay of worker threads from `random.uniform(0.05, 0.15)` to `random.uniform(0.01, 0.04)` since warmed SSL/TLS connections in the pool prevent concurrent TLS handshaking resource/socket lockups.
- Developed the `@st.cache_data(ttl=3600)` decorated `_pre_warm_resources(pro_id)` helper function in `pages/analytics.py` executing at page load time. This caches the warming routine hourly, pre-warming connections and pre-fetching program, session, and exam metadata in parallel on initial dashboard load.
- Added a robust unit test `test_connection_pre_warming` in `tests/test_full_system.py` using mocks to verify exact concurrent GET and HEAD calls.
- Verified that all unit and system integration tests pass successfully (21/21 tests passed).
- Modified files: `cli_scraper.py`, `pages/analytics.py`, `tests/test_full_system.py`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Parallelizing cold HTTP/SSL connection pre-warming using a thread pool before worker thread spawn solves the performance bottleneck of requests/urllib3 connection setup contention under the GIL.
  - Using Streamlit's `@st.cache_data` at page load to execute background warming operations ensures interactions are responsive (<3-4s cold, ~2s warm) without blocking consecutive Streamlit reruns.
  - Stagger delays are only useful when threads perform fresh TLS handshakes; with pre-warmed pooled connections, thread launching can be virtually instantaneous, drastically speeding up multi-threaded batch operations.
---
## 2026-06-01 02:15 - Completion of US-013
- Implemented standard-library atomic directory lock context manager `file_process_lock` utilizing `os.mkdir()` which is inherently atomic across OS processes and threads on both Windows and Linux, preventing JSON state database corruption under parallel runs.
- Wrapped the entire loading, processing (readd detection), auto-promotion, and saving operations of `saved_profiles.json` under a single critical section using the process-safe lock to eliminate lost updates/race conditions.
- Protected `v2_sync_tasks.json` writing operations with the process-safe lock.
- Initialized `samples = []` inside the candidate iteration loop in `identify_batch_for_exam` inside `auto_pdf_mailer.py` to prevent `NameError` or leaked student state when candidate batch profiles have empty lists of registrations.
- Configured a 15-second explicit network request timeout inside `urllib.request.urlopen` calls in `find_latest.py` and `sync_state.py` to eliminate potential infinite hangs on network page drops.
- Cleaned up bare `except:` blocks by refactoring to standard `except Exception:` catches, maintaining normal `KeyboardInterrupt` / Ctrl+C and exit signal handling.
- Silenced Python 3.12+ runtime deprecation warnings by replacing `datetime.utcnow()` with timezone-aware `datetime.now(timezone.utc)` in `monitor.py`.
- Fortified standard import path resolution of `auto_pdf_mailer` by dynamically inserting `monitor_dir` explicitly into `sys.path`.
- Modified files: `exam_monitor/auto_pdf_mailer.py`, `exam_monitor/monitor.py`, `exam_monitor/find_latest.py`, `exam_monitor/sync_state.py`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Standard in-memory locks like `threading.Lock` are completely ineffective across separate background processes. Utilizing directories (`os.mkdir`) for atomic locking is a clean, zero-dependency, and cross-platform way to serialize access to files without external binaries.
  - Doing file reads outside of a lock block and writing under a lock is a classic race condition. Both the read and write operations must reside within the same lock boundary to prevent dirty writes or lost updates.
  - Never omit timeouts inside standard Python `urllib` network calls, as it defaults to an infinite duration which can freeze automated execution runners indefinitely.
  - Explicitly configuring search directories inside `sys.path` guarantees script importability regardless of the current working directory context from which Python is executed.
---
## 2026-06-01 02:42 - Completion of US-014
- Created a completely isolated monitoring suite inside the `portal_monitor/` directory containing `health_check.py` and `state.json` to keep the results-checking system unencumbered and clean.
- Implemented robust **Positive Verification (White-listing)** verifying that the server successfully responds with HTTP `200 OK` and returns standard DUCMC signatures (`"DUCMC"` and `"University of Dhaka"`).
- Correctly classified all other network errors, connection timeouts, gateway failures, custom database failures, or WAF screens (such as the `"CrowdSec Access Forbidden"` page) as **OFFLINE** (down) to ensure total coverage.
- Configured a standalone `portal_health.yml` GitHub Actions workflow scheduled every 10 minutes via cron.
- Implemented statefulness across Actions runners using `actions/cache` with dynamic keys and fallback prefixes, retrieving and saving `state.json` cleanly on each run with zero commit pollution.
- Configured SMTP notifications to trigger strictly on status transition changes, routed exclusively to `fahimfaisal2657@gmail.com`.
- Added manual force flags (`--force-online` and `--force-offline`) and `--test-email` flag to make verification simple.
- Verified that the check script successfully runs locally, diagnosing standard portal uptime and exiting silently when status is unchanged.
- Modified files: `portal_monitor/health_check.py`, `portal_monitor/state.json`, `.github/workflows/portal_health.yml`, `.gitignore`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Standard white-list verification (Positive Verification) is infinitely more reliable than error-pattern black-listing for health checks, as it encapsulates all possible timeout, gateway, WAF, or blank page errors under a single expected healthy signature.
  - Using Actions Cache with dynamic restore-keys allows storing mutable operational state (like JSON status files) in GitHub Actions workflows without generating high-volume Git history noise.
---
## 2026-06-28 17:30 - Diagnosis and Restoration of Missing CSE 08 4-1 Exam
- Diagnosed database sync gap: The CSE 08 4-1 results (Exam ID `1769`) were successfully auto-synced to the database on May 21 (`233f7e4`) but subsequently clobbered on June 1 (`88c41cf` and `134be22`) when an outdated database was committed and pushed locally.
- Restored missing CSE 08 4-1 results back into `result_finder.db` by manually queueing the task in the temp directory and running `v2_auto_sync.py`.
- Flushed the SQLite Write-Ahead Log (WAL) to disk via checkpointing to ensure the modifications are saved directly to `result_finder.db` and tracked by Git.
- Ran offline database comparison against commit `233f7e4` to verify that no other historical exam data was lost.
- Verified that all 27 unit and system tests pass successfully.
- Modified files: `result_finder.db`, `.coderabbit/review_report.md`, `progress.txt`.
- **Learnings for future iterations:**
  - Committing binary database files (`result_finder.db`) directly can easily clobber remote changes if local checkouts are out of sync. Always pull before pushing, and verify remote database state.
  - SQLite Write-Ahead Logging (WAL) writes modifications to the temporary `-wal` file. Running `PRAGMA wal_checkpoint(TRUNCATE)` is necessary to merge and save those changes into the main `.db` file for Git tracking.

## 2026-07-03 05:25 - US-015: Special Exam Indicators with Readmitted Student Fix
- Implemented `build_special_exam_lookup(dept)` in `database.py` returning published main exam years per semester.
- Refactored `get_batch_first_participation_years(profile_name)` in `database.py` to select the exam per semester with the highest student count (`COUNT(reg_no)`), preventing readmitted students' historical exam records from skewing the batch's baseline participation year.
- Updated `compute_advanced_projection()` to calculate and attach `is_special` to both pending retakes and improvement candidates.
- Integrated lookup calls at `analytics.py` call site, caching the results per department in Streamlit session state to maximize paging performance.
- Modified `_render_semester_grouped()` in `analytics.py` to append `:red[(Special)]` to the group semester headers if any candidate requires special application.
- Added a comprehensive unit test `test_get_batch_first_participation_years_highest_count` in `tests/test_full_system.py`.
- Conducted Code Rabbit code review and generated `.coderabbit/review_report.md`.
- Verified that all 22 tests in the test suite pass successfully.
- Modified files: `database.py`, `pages/analytics.py`, `tests/test_full_system.py`, `.coderabbit/review_report.md`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Grouping by `exam_id, exam_name` and using `COUNT(reg_no)` is a robust way to identify the true main exam for a batch cohort, filtering out outlier records from readmitted students in single SQL queries.
  - Streamlit `:color[text]` markup syntax allows adding colored labels (like red badges) to section headers without needing raw HTML rendering flags.
---
